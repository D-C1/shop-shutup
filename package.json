{
  "name": "shop-shutup",
  "version": "1.0.1-beta",
  "scripts": {
    "build": "webpack --mode production",
    "dev": "webpack --mode development --watch",
    "release": "npm run build && git tag -a v$npm_package_version -m \"Release v$npm_package_version\""
  },
  "devDependencies": {
    "copy-webpack-plugin": "^11.0.0",
    "webpack": "^5.88.0",
    "webpack-cli": "^5.1.4"
  }
}
